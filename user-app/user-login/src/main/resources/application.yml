spring:
  application:
    name: user-login

  datasource:
    url: jdbc:mysql://localhost:3306/login_database
    username: root
    password: Raja19492000
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8080
